namespace Data.Enum
{
    // TODO: Move to Infrastructure project if needed
    public enum OrderStatus
    {
        InProgress,
        Confirmed,
        Shipping,
        Success,
        Canceled
    }
}